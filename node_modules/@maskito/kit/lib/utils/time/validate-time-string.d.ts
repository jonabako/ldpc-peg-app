import { MaskitoTimeSegments } from '../../types';
export declare function validateTimeString({ timeString, paddedMaxValues, offset, selection: [from, to], }: {
    timeString: string;
    paddedMaxValues: MaskitoTimeSegments;
    offset: number;
    selection: readonly [number, number];
}): {
    validatedTimeString: string;
    updatedTimeSelection: [number, number];
};
//# sourceMappingURL=validate-time-string.d.ts.map