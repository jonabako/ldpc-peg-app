import { Options as PrettierOptions } from 'prettier';
interface Options {
    config: PrettierOptions;
    file: string;
}
export declare function tuiPostPrettierFormat({ file, config }: Options): Promise<void>;
export {};
