import { ChangeDetectorRef, Injector, OnDestroy, TemplateRef } from '@angular/core';
import { TuiDropdownPortalService } from '@taiga-ui/cdk';
import { TuiHorizontalDirection } from '@taiga-ui/core';
import { PolymorpheusTemplate } from '@tinkoff/ng-polymorpheus';
import * as i0 from "@angular/core";
export declare class TuiSidebarDirective<T = Record<string, unknown>> extends PolymorpheusTemplate<T> implements OnDestroy {
    readonly content: TemplateRef<T>;
    private readonly injector;
    private readonly portalService;
    private readonly component;
    private sidebarRef;
    direction: TuiHorizontalDirection;
    autoWidth: boolean;
    set tuiSidebar(open: boolean);
    constructor(content: TemplateRef<T>, injector: Injector, portalService: TuiDropdownPortalService, cdr: ChangeDetectorRef);
    ngOnDestroy(): void;
    private show;
    private hide;
    static ɵfac: i0.ɵɵFactoryDeclaration<TuiSidebarDirective<any>, never>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<TuiSidebarDirective<any>, "[tuiSidebar]", never, { "direction": "tuiSidebarDirection"; "autoWidth": "tuiSidebarAutoWidth"; "tuiSidebar": "tuiSidebar"; }, {}, never>;
}
