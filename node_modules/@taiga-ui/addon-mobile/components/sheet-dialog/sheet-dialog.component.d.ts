import { AfterViewInit, ElementRef } from '@angular/core';
import { TuiDialog } from '@taiga-ui/cdk';
import { TuiCommonIcons } from '@taiga-ui/core';
import { BehaviorSubject, Observable } from 'rxjs';
import { TuiSheetDialogOptions } from './sheet-dialog.options';
import * as i0 from "@angular/core";
export declare class TuiSheetDialogComponent<I> implements AfterViewInit {
    private readonly el;
    private readonly duration;
    readonly icons: TuiCommonIcons;
    readonly closeWord$: Observable<string>;
    readonly context: TuiDialog<TuiSheetDialogOptions<I>, any>;
    private readonly sheet?;
    private readonly stopsRefs;
    private pointers;
    readonly slideInTop: {
        value: string;
        params: {
            start: string;
            duration: number;
        };
    };
    stuck$: BehaviorSubject<boolean>;
    constructor(el: ElementRef<HTMLElement>, duration: number, icons: TuiCommonIcons, closeWord$: Observable<string>, context: TuiDialog<TuiSheetDialogOptions<I>, any>);
    get offset(): number;
    get closeable(): boolean;
    get isSmall(): boolean;
    onPointerChange(delta: number): void;
    close(): void;
    ngAfterViewInit(): void;
    private get sheetTop();
    private getStops;
    static ɵfac: i0.ɵɵFactoryDeclaration<TuiSheetDialogComponent<any>, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<TuiSheetDialogComponent<any>, "tui-sheet-dialog", never, {}, {}, never, never>;
}
