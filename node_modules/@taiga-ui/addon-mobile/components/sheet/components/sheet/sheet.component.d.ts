import { AfterViewInit, ElementRef, NgZone } from '@angular/core';
import { Observable } from 'rxjs';
import { TuiSheet, TuiSheetRequiredProps } from '../../sheet';
import * as i0 from "@angular/core";
export declare class TuiSheetComponent<T> implements TuiSheetRequiredProps<T>, AfterViewInit {
    private readonly scroll$;
    private readonly el;
    private readonly zone;
    readonly isIos: boolean;
    readonly moreWord$: Observable<string>;
    private readonly destroy$;
    private readonly sheet?;
    private readonly content?;
    private readonly stopsRefs;
    item: TuiSheet<T>;
    id: string;
    readonly stuck$: Observable<boolean>;
    constructor(scroll$: Observable<number>, el: ElementRef<HTMLElement>, zone: NgZone, isIos: boolean, moreWord$: Observable<string>, destroy$: Observable<void>);
    get stops(): readonly number[];
    get imageStop(): number;
    get imageHeight(): number;
    get context(): TuiSheet<T>;
    onId(id: string): void;
    ngAfterViewInit(): void;
    scrollTo(top?: number): void;
    close(): void;
    private get contentTop();
    private get sheetTop();
    private getStops;
    static ɵfac: i0.ɵɵFactoryDeclaration<TuiSheetComponent<any>, [null, null, null, null, null, { self: true; }]>;
    static ɵcmp: i0.ɵɵComponentDeclaration<TuiSheetComponent<any>, "tui-sheet", never, { "item": "item"; }, {}, never, never>;
}
