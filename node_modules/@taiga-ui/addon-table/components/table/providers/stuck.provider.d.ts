import { InjectionToken, Provider } from '@angular/core';
/**
 * Stream of sticky stuck events
 */
export declare const TUI_STUCK: InjectionToken<boolean>;
export declare const TUI_STUCK_PROVIDER: Provider;
