import { TuiBrightness } from '@taiga-ui/core';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { Observer } from 'rxjs';
import { TuiTableBarOptions } from '../interfaces/table-bar-options';
export declare class TuiTableBar {
    private readonly observer;
    readonly content: PolymorpheusContent<Record<string, any>>;
    readonly adaptive: boolean;
    readonly hasCloseButton: boolean;
    readonly mode: TuiBrightness;
    constructor(observer: Observer<never>, content: PolymorpheusContent<Record<string, any>>, options?: TuiTableBarOptions);
    close(): void;
}
